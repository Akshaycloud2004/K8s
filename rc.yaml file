apiVersion: v1
kind: ReplicationController
metadata:
    name: rc-name
spec:
   replicas: 3
   selector:
        env: app
   template:
       metadata:
           name: tm1
           labels:
              env: app
       spec:
          containers:
              - name: c1
                image: nginx
                ports:
                  - containerPort: 80
